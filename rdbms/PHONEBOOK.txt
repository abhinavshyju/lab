CREATE TABLE PHONEBOOK(PNAME VARCHAR(25),MOBNO VARCHAR(10));
 INSERT INTO PHONEBOOK VALUES('ARJUN',8025321236);
 INSERT INTO PHONEBOOK VALUES('ARJU',8025321235);
 INSERT INTO PHONEBOOK VALUES('ARJ',8025321234);
 INSERT INTO PHONEBOOK VALUES('AR',8025321233);
 CREATE OR REPLACE FUNCTION CONTACT() RETURNS TRIGGER AS
 '
 DECLARE
 BEGIN
 INSERT INTO DELPHONEBOOK
VALUES(OLD.PNAME,OLD.MOBNO,CURRENT_TIME);
 RETURN OLD;
 END;
 'language 'plpgsql';
 CREATE TRIGGER STOREDELETED
 AFTER DELETE ON PHONEBOOK
 FOR EACH ROW EXECUTE PROCEDURE CONTACT();